from project root run pytest
pytest automatically adds project root to sys.path
allows imports lik

inside tests/
DO
from my_package.module_a import some_function
DONOT
from src.my_package.module_a import some_function

Run everything from project root
Treat src as a package (not a folder of scripts)
Stop cross-dependencies from src → tests